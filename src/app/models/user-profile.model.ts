export interface UserProfile {
  firstName: string;
  lastName: string;
  address: string;
  region?: string;
  officeLocation: string;
  phoneNumber: string;
  emailAddress: string;
  postalCode: string;
  id?: number;
}
